Run this as a fresh, isolated comparison. Use the same supplied inputs and acceptance criteria for every condition. Never inspect another condition's outputs. Record the actual model, provider, effort, elapsed time, token usage and billed or calculated inference cost. Separate API receipts from subscription usage and media credits. Do not invent missing usage or label subscription access free. Preserve the first result and any repairs. Return the actual deliverable, evidence of the requested checks and a short account of remaining defects. Stop after 25 minutes and deliver the current state if incomplete. This is a test, so a failed result is useful evidence.

Start from an unchanged copy of results/astra-raw-high-64k/index.html, Jack's preferred initial Astra design. Give each condition that same source and its desktop screenshot. Preserve the accepted hero, typography, spacing and existing interactions.

Add a settings screen using the existing visual system. Implement profile display-name editing, light/dark appearance and a reset action. Persist settings locally across reload. Validate an empty display name and show visible inline feedback. Navigation must reach the new screen and return to the original page. Use local demo data only; no real authentication or external account changes.

Use the supplied Design Loop skill for relevant visual checks, limited to this desktop change. Provide exact before/after browser observations of every new control, validation, reload persistence and reset. Include one screenshot of the original hero after the change to show whether it was preserved. If the permitted review/repair budget ends before the skill's pass criteria, report the unmet criteria rather than claiming a pass. Do not regenerate artwork or rewrite the whole original page unnecessarily.

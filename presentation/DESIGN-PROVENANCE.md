# DeepAstra presentation references

The approved reference is Jack’s September 10 presentation, “Build beautiful websites. With GPT-6 Astra.” The current deck retains its local Inter and Newsreader font files and editorial layout. The Huly-inspired opening beam is preserved. React Bits LaserFlow attribution and its MIT + Commons Clause licence remain in assets/react-bits-license.md.

Revision 14 keeps model identities in fixed positions across all eight benchmark views: Astra, Sol, Grok, DeepSeek. Logos sit below the chart rather than inside the variable-height bars. DeepSeek remains animated blue while competitors remain grey. The benchmark values are unchanged. Actual benchmark names and methodology notes are shown beneath the plots.

The repository scene illustrates DeepSeek inside the Codex CLI, with Astra providing a brief and reviewing the result. The separate open-model scene explains the MIT release and distinguishes a local harness from hosted inference. A switch illustrates self-hosting. The 10 September release scene includes the vendor’s 54.4% to 74.2% DeepSWE comparison, explicitly labelled maximum reasoning rather than our High-setting experiments. Official model cards and pricing links appear in each relevant disclosure.

The cost highlight retains the authorized Higgsfield glass film. It was cropped to the relevant glass area, interpolated through optical flow at 120fps, and sampled on a smooth closed cosine cycle. The delivered MP4 runs for 24 seconds at 60fps. A separate bright blue gradient stays beneath the film with screen blending. This avoids the black background Jack rejected. The browser uses native looping rather than seeking the video back in JavaScript. Numerical endpoint and cadence checks are retained in loop-validation.json. Playback pauses through the existing motion controls.

The three use-case illustrations were generated through Higgsfield with Nano Banana Pro, requested at 2K and 16:9. Each topic received three variants, with the retained OpenAI knot and DeepSeek whale supplied as references. The design and OS jobs also received the relevant supplied website or Business UI screenshot. The selected images are design variant 2, business variant 3 and OS variant 2. The other variants remain on Jack’s Desktop under Infographics - DeepAstra. Business variant 1 duplicated an output report and was rejected; OS variants 1 and 3 introduced unwanted UI numbers, so variant 2 was selected. Illustrations explain the workflow; original evidence and example links remain available. Selection metadata is in infographics.json.

The supplied DeepSeek example is served from studies/design-examples/deepseek so a normal browser link can open it. The original local directory is unchanged. The separate Bricks example links to its existing localhost server. Neither supplied example has been relabelled as a controlled experiment.

Pricing uses real bar graphs for input, output, and a combined usage example. The combined example means 1M uncached input plus 1M output across requests with at most 272K input tokens each. Peak/off-peak changes DeepSeek’s tariff. Astra and Sol prices remain fixed for the displayed standard/promotion rates. API rate multiples do not claim equal-quality task savings. Sources disclose long-context surcharges and Sol’s promotion.

SlopMonster was run before and after editing. The original lint score was 4/5; final copy passes at 5/5 with the evidenced-number flag. The Claude CLI review was unavailable because its OAuth session expired, so DeepSeek provided the required separate-family review through its API. The review preserved facts; final graphic labels were inspected visually. No experimental outputs were rewritten.

The requested reusable treatment is linked from the presentation-tiles skill. Its blue highlight component is at ~/.codex/skills/presentation-tiles/assets/deepastra-highlight, with guidance in references/deepastra-treatment.md. This is a reusable option, not a rule imposed on unrelated decks.

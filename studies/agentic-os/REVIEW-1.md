Astra desktop review of your completed dashboard found a concrete visual mismatch: on a fresh light-theme browser, the dashboard forces its main content dark but leaves the existing sidebar white with nearly invisible light text, while the reference Business page renders its sidebar dark correctly regardless of the saved global theme. Match Business's scoped sidebar theme tokens for the dashboard as well; preserve the user theme preference and the Business route. The second half of the greeting is extremely faint on the new dark background: make the supporting headline comfortably readable while keeping the Business hierarchy. Also add vertical breathing room between the KPI row and the Dream Review panel; current overview cards touch the Dream panel edge. Make these focused design-system corrections as the sole DeepSeek author. Do not redesign other sections, call APIs or inspect private data. Read your existing dashboard.css/dashboard.tsx and Business CSS, make minimal appropriate changes, run the build once, update deepastra-result.md. You have 150seconds. Supervisor is independently fixing environment-only dependency isolation and will handle browser clicks. No shell browser automation. Do not alter the source repository outside this worktree or commit/merge/publish anything.